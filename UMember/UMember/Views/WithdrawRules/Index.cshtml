@model IEnumerable<UMember.tbWithdrawRules>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Limit_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lower_Money)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.uper_Money)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HandlingFee)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Limit_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lower_Money)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.uper_Money)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HandlingFee)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
